import React from "react";
import Layout from "../components/layout";

const Home = ({ children }) => {
  return (
    <div>
      <Layout>HEllo page</Layout>
    </div>
  );
};

export default Home;
